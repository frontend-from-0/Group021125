# Restaurant Order Queue — Lesson Implementation Plan

# Final project features

* Add a new order.
* See all active orders.
* Move an order to the next status.
* Delete an order.
* See simple statistics:

  * total orders
  * active orders
  * served orders

Optional:

* Filter orders by status (Received → Preparing → Ready → Served)
* Save orders in `localStorage`.

